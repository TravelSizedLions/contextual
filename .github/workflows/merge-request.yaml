name: Merge Request
run-name: '#${{github.event.pull_request.number}}: "${{github.event.pull_request.title}}" [${{github.actor}}]'
on:
  pull_request:
    branches: ["dev"]
  push:
    branches: ["dev"]

jobs:
  unit-tests:
    uses: TravelSizedLions/actions/test-drive.yaml@dev
